@model IEnumerable<VuLinh_BTH2_17_11.Models.Faculty>
@{
    ViewData["Title"] = "Index";
}
<h1>Index</h1>
<p>
    <a asp-action="Create">Create new</a>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FacultyID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FacultyName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayNameFor(modeltem => item.FacultyName)
                </td>
                <td>
                    @Html.DisplayNameFor(modeltem => item.FacultyID)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.FacultyID">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.FacultyID">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.FacultyID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
